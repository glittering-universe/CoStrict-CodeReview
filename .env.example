# Note: `.env` is NOT loaded by default. To enable dotenv loading, set `SHIPPIE_LOAD_DOTENV=true`
# in your shell before starting shippie (e.g. `SHIPPIE_LOAD_DOTENV=true bun run start:web`).

OPENAI_API_KEY="YOUR_API_KEY"
OPENAI_API_BASE="https://open.bigmodel.cn/api/paas/v4"

# Bun server idle timeout (seconds). Increase this for long-running SSE review sessions.
SHIPPIE_SERVER_IDLE_TIMEOUT=120

# SSE heartbeat interval (ms). Keep this lower than any proxy/server idle timeout.
SHIPPIE_SSE_PING_INTERVAL_MS=5000
